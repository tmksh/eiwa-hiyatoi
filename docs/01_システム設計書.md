# 日雇い管理・賃金計算システム 設計書

## 1. システム概要

### 1.1 システム名
**永和日雇い管理システム（Eiwa Hiyatoi Management System）**

### 1.2 目的
日々約200名分の給与計算を、現行業務フローをほぼ変えずにシステム化し、
複雑な単価・残業計算ルールを正確かつ一括で処理する。

### 1.3 解決する課題
| 課題 | システムによる解決 |
|------|-------------------|
| 車種・会社ごとに基本給、残業単価、計算ルールが異なる | マスタデータで柔軟に設定管理 |
| 毎日200名分の給与計算が手作業で負担大 | 一括自動計算機能 |
| 計算後の修正（手直し）が多く再計算が大変 | 履歴保存＋ワンクリック再計算 |
| 業務フローを変えるのが難しい | 現行フローに沿ったUI設計 |

---

## 2. システムアーキテクチャ

### 2.1 技術スタック

```
┌─────────────────────────────────────────────────────────────┐
│                      Frontend                                │
│   Next.js 14 (App Router) + TypeScript + Tailwind CSS       │
│   shadcn/ui コンポーネント                                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Backend API                             │
│   Next.js API Routes (Server Actions)                        │
│   Prisma ORM + PostgreSQL                                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Database                                │
│   PostgreSQL (Supabase or Railway)                          │
│   計算履歴・マスタデータ・日報データ                           │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 選定理由

| 技術 | 選定理由 |
|------|----------|
| **Next.js 14** | フルスタック対応、Server Actionsで高速なデータ処理 |
| **TypeScript** | 型安全性により計算ロジックのバグを防止 |
| **Prisma** | 型安全なDB操作、マイグレーション管理が容易 |
| **PostgreSQL** | JSON型でルール設定を柔軟に保存、計算精度が高い |
| **shadcn/ui** | モダンで一貫性のあるUI、カスタマイズ性が高い |

---

## 3. データベース設計

### 3.1 ER図（概念）

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Company    │────<│ VehicleType  │────<│  WageRule    │
│   (会社)     │     │  (車種)      │     │ (賃金ルール) │
└──────────────┘     └──────────────┘     └──────────────┘
       │                    │
       │                    │
       ▼                    ▼
┌──────────────┐     ┌──────────────┐
│   Worker     │────<│  DailyReport │
│   (作業員)   │     │   (日報)     │
└──────────────┘     └──────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │WageCalculation│
                    │  (計算結果)   │
                    └──────────────┘
```

### 3.2 テーブル定義

#### 3.2.1 companies（会社マスタ）
```sql
CREATE TABLE companies (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code            VARCHAR(20) UNIQUE NOT NULL,  -- 会社コード
  name            VARCHAR(100) NOT NULL,         -- 会社名
  overtime_unit   INTEGER NOT NULL DEFAULT 15,   -- 残業計算単位（分）: 5, 10, 15, 30
  rounding_method VARCHAR(20) DEFAULT 'floor',   -- 端数処理: floor(切捨), ceil(切上), round(四捨五入)
  restraint_rule  JSONB,                         -- 拘束時間ルール（後述）
  is_active       BOOLEAN DEFAULT true,
  created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.2.2 vehicle_types（車種マスタ）
```sql
CREATE TABLE vehicle_types (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id      UUID REFERENCES companies(id),
  code            VARCHAR(20) NOT NULL,          -- 車種コード
  name            VARCHAR(50) NOT NULL,          -- 車種名（2t, 4t, 10t等）
  display_order   INTEGER DEFAULT 0,             -- 表示順
  is_active       BOOLEAN DEFAULT true,
  created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(company_id, code)
);
```

#### 3.2.3 wage_rules（賃金ルールマスタ）★最重要
```sql
CREATE TABLE wage_rules (
  id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id            UUID REFERENCES companies(id),
  vehicle_type_id       UUID REFERENCES vehicle_types(id),
  effective_from        DATE NOT NULL,              -- 適用開始日
  effective_to          DATE,                       -- 適用終了日（NULLは無期限）

  -- 基本給関連
  base_daily_wage       DECIMAL(10,0) NOT NULL,     -- 日給（基本）
  base_hours            DECIMAL(4,2) DEFAULT 8.00,  -- 基本労働時間

  -- 残業関連
  overtime_rate_normal  DECIMAL(10,0),              -- 残業単価（通常）
  overtime_rate_late    DECIMAL(10,0),              -- 残業単価（深夜）
  overtime_rate_holiday DECIMAL(10,0),              -- 残業単価（休日）

  -- 拘束時間関連
  restraint_start_hour  INTEGER,                    -- 拘束開始判定時間
  restraint_allowance   DECIMAL(10,0),              -- 拘束手当

  -- その他手当
  allowances            JSONB DEFAULT '{}',         -- 追加手当設定

  -- メタ情報
  rule_description      TEXT,                       -- ルール説明メモ
  is_active             BOOLEAN DEFAULT true,
  created_at            TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at            TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.2.4 workers（作業員マスタ）
```sql
CREATE TABLE workers (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  employee_code   VARCHAR(20) UNIQUE NOT NULL,   -- 従業員番号
  name            VARCHAR(50) NOT NULL,          -- 氏名
  name_kana       VARCHAR(50),                   -- フリガナ
  default_company_id UUID REFERENCES companies(id), -- 主な派遣先
  phone           VARCHAR(20),
  notes           TEXT,
  is_active       BOOLEAN DEFAULT true,
  created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.2.5 daily_reports（日報）
```sql
CREATE TABLE daily_reports (
  id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  work_date         DATE NOT NULL,                -- 作業日
  worker_id         UUID REFERENCES workers(id),
  company_id        UUID REFERENCES companies(id),
  vehicle_type_id   UUID REFERENCES vehicle_types(id),

  -- 時間情報
  start_time        TIME NOT NULL,                -- 出勤時間
  end_time          TIME NOT NULL,                -- 退勤時間
  break_minutes     INTEGER DEFAULT 60,           -- 休憩時間（分）

  -- 計算用フラグ
  is_holiday        BOOLEAN DEFAULT false,        -- 休日出勤フラグ
  is_night_shift    BOOLEAN DEFAULT false,        -- 深夜勤務フラグ

  -- 手動調整
  manual_adjustments JSONB DEFAULT '{}',          -- 手動調整内容

  -- ステータス
  status            VARCHAR(20) DEFAULT 'draft',  -- draft, calculated, confirmed, paid
  notes             TEXT,

  created_at        TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at        TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_by        UUID,

  UNIQUE(work_date, worker_id)
);
```

#### 3.2.6 wage_calculations（計算結果履歴）★履歴管理
```sql
CREATE TABLE wage_calculations (
  id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  daily_report_id     UUID REFERENCES daily_reports(id),
  calculation_version INTEGER NOT NULL DEFAULT 1,  -- 計算バージョン（再計算時にインクリメント）

  -- 適用ルール（スナップショット）
  applied_rule_id     UUID REFERENCES wage_rules(id),
  applied_rule_snapshot JSONB NOT NULL,            -- 計算時点のルールを保存

  -- 計算結果
  work_minutes        INTEGER NOT NULL,            -- 実労働時間（分）
  overtime_minutes    INTEGER DEFAULT 0,           -- 残業時間（分）
  night_minutes       INTEGER DEFAULT 0,           -- 深夜時間（分）

  base_wage           DECIMAL(10,0) NOT NULL,      -- 基本給
  overtime_wage       DECIMAL(10,0) DEFAULT 0,     -- 残業代
  night_wage          DECIMAL(10,0) DEFAULT 0,     -- 深夜手当
  holiday_wage        DECIMAL(10,0) DEFAULT 0,     -- 休日手当
  restraint_allowance DECIMAL(10,0) DEFAULT 0,     -- 拘束手当
  other_allowances    DECIMAL(10,0) DEFAULT 0,     -- その他手当

  total_wage          DECIMAL(10,0) NOT NULL,      -- 合計支給額

  -- 計算詳細ログ
  calculation_log     JSONB NOT NULL,              -- 計算過程の詳細

  -- メタ情報
  is_current          BOOLEAN DEFAULT true,        -- 現在有効な計算結果か
  calculated_at       TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  calculated_by       UUID
);
```

### 3.3 拘束時間ルールのJSONB構造例

```json
{
  "type": "threshold",
  "conditions": [
    {
      "min_hours": 12,
      "max_hours": 14,
      "allowance": 2000
    },
    {
      "min_hours": 14,
      "max_hours": null,
      "allowance": 4000
    }
  ],
  "includes_break": false
}
```

---

## 4. 計算エンジン設計

### 4.1 計算フロー

```
┌─────────────────┐
│   日報データ     │
│  (入力値)       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ ルール取得       │ ← 会社×車種×日付で該当ルールを特定
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 労働時間計算     │ ← 実労働時間 = 退勤 - 出勤 - 休憩
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 残業時間計算     │ ← 会社の残業単位で丸め処理
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 各種手当計算     │ ← 拘束手当、深夜手当等
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 合計算出        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 履歴保存        │ ← 計算過程をJSONで保存
└─────────────────┘
```

### 4.2 計算ロジック（TypeScript）

```typescript
// types/wage.ts
interface WageCalculationInput {
  workDate: Date;
  workerId: string;
  companyId: string;
  vehicleTypeId: string;
  startTime: string;      // "HH:mm"
  endTime: string;        // "HH:mm"
  breakMinutes: number;
  isHoliday: boolean;
  isNightShift: boolean;
  manualAdjustments?: ManualAdjustment[];
}

interface WageCalculationResult {
  workMinutes: number;
  overtimeMinutes: number;
  nightMinutes: number;
  baseWage: number;
  overtimeWage: number;
  nightWage: number;
  holidayWage: number;
  restraintAllowance: number;
  otherAllowances: number;
  totalWage: number;
  calculationLog: CalculationLogEntry[];
}

// lib/wage-calculator.ts
export class WageCalculator {

  /**
   * 残業時間を会社ルールに従って丸める
   */
  private roundOvertimeMinutes(
    minutes: number,
    unit: number,
    method: 'floor' | 'ceil' | 'round'
  ): number {
    switch (method) {
      case 'floor':
        return Math.floor(minutes / unit) * unit;
      case 'ceil':
        return Math.ceil(minutes / unit) * unit;
      case 'round':
        return Math.round(minutes / unit) * unit;
    }
  }

  /**
   * メイン計算処理
   */
  async calculate(input: WageCalculationInput): Promise<WageCalculationResult> {
    const log: CalculationLogEntry[] = [];

    // 1. 該当する賃金ルールを取得
    const rule = await this.getApplicableRule(
      input.companyId,
      input.vehicleTypeId,
      input.workDate
    );
    log.push({ step: 'ルール取得', detail: rule });

    // 2. 実労働時間を計算
    const workMinutes = this.calculateWorkMinutes(
      input.startTime,
      input.endTime,
      input.breakMinutes
    );
    log.push({ step: '実労働時間', value: workMinutes, unit: '分' });

    // 3. 残業時間を計算（丸め処理込み）
    const baseMinutes = rule.baseHours * 60;
    const rawOvertimeMinutes = Math.max(0, workMinutes - baseMinutes);
    const overtimeMinutes = this.roundOvertimeMinutes(
      rawOvertimeMinutes,
      rule.company.overtimeUnit,
      rule.company.roundingMethod
    );
    log.push({
      step: '残業時間',
      raw: rawOvertimeMinutes,
      rounded: overtimeMinutes,
      unit: rule.company.overtimeUnit,
      method: rule.company.roundingMethod
    });

    // 4. 基本給
    const baseWage = rule.baseDailyWage;
    log.push({ step: '基本給', value: baseWage });

    // 5. 残業代計算
    const overtimeRate = input.isHoliday
      ? rule.overtimeRateHoliday
      : rule.overtimeRateNormal;
    const overtimeWage = Math.floor((overtimeMinutes / 60) * overtimeRate);
    log.push({ step: '残業代', minutes: overtimeMinutes, rate: overtimeRate, value: overtimeWage });

    // 6. 拘束手当計算
    const totalMinutes = this.calculateTotalMinutes(input.startTime, input.endTime);
    const restraintAllowance = this.calculateRestraintAllowance(
      totalMinutes,
      rule.company.restraintRule
    );
    log.push({ step: '拘束手当', totalMinutes, value: restraintAllowance });

    // 7. 合計
    const totalWage = baseWage + overtimeWage + restraintAllowance;

    return {
      workMinutes,
      overtimeMinutes,
      nightMinutes: 0, // 深夜計算は別途
      baseWage,
      overtimeWage,
      nightWage: 0,
      holidayWage: 0,
      restraintAllowance,
      otherAllowances: 0,
      totalWage,
      calculationLog: log
    };
  }
}
```

---

## 5. 画面設計

### 5.1 画面一覧

| No | 画面名 | 機能概要 | 優先度 |
|----|--------|----------|--------|
| 1 | ダッシュボード | 本日の処理状況サマリー | 高 |
| 2 | 日報入力 | 日報の新規登録・編集 | 高 |
| 3 | 一括計算 | 日付指定で一括賃金計算 | 高 |
| 4 | 計算結果一覧 | 計算結果の確認・修正 | 高 |
| 5 | 会社マスタ | 会社情報・残業ルール管理 | 中 |
| 6 | 車種マスタ | 車種情報管理 | 中 |
| 7 | 賃金ルールマスタ | 賃金ルール設定 | 中 |
| 8 | 作業員マスタ | 作業員情報管理 | 中 |
| 9 | 履歴照会 | 計算履歴の参照 | 低 |
| 10 | CSV出力 | 給与システム連携用出力 | 中 |

### 5.2 画面遷移図

```
                    ┌─────────────┐
                    │  ログイン    │
                    └──────┬──────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────┐
│                    ダッシュボード                          │
│  ・本日の未処理件数  ・計算済み件数  ・要確認件数           │
└──────────────────────────────────────────────────────────┘
        │           │           │           │
        ▼           ▼           ▼           ▼
   ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
   │日報入力 │  │一括計算 │  │結果一覧│  │マスタ  │
   └────────┘  └────────┘  └────────┘  └────────┘
```

### 5.3 主要画面ワイヤーフレーム

#### 5.3.1 ダッシュボード
```
┌────────────────────────────────────────────────────────────┐
│ 🟡 永和日雇い管理システム                    [管理者名] ▼  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐       │
│  │  📝 未入力    │ │  ⏳ 計算待ち  │ │  ✅ 完了     │       │
│  │     23名     │ │     45名     │ │    132名     │       │
│  └──────────────┘ └──────────────┘ └──────────────┘       │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ クイックアクション                                    │  │
│  │  [📋 日報入力]  [🔄 一括計算]  [📊 結果確認]         │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 最近の計算結果（要確認）                              │  │
│  │ ┌────────────────────────────────────────────────┐  │  │
│  │ │ 山田太郎 | A社 | 4t | ¥15,400 | ⚠️ 残業14h超  │  │  │
│  │ │ 鈴木一郎 | B社 | 2t | ¥12,800 | ⚠️ 手動調整有 │  │  │
│  │ └────────────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

#### 5.3.2 日報入力画面
```
┌────────────────────────────────────────────────────────────┐
│ ← 戻る          日報入力              2024年1月28日（月）   │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  作業日: [2024/01/28 📅]                                   │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 作業員検索: [         🔍]                            │  │
│  │ または一覧から選択 ▼                                  │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  派遣先会社: [A運輸株式会社        ▼]                      │
│  車種:       [4tトラック           ▼]                      │
│                                                            │
│  ┌───────────────┐  ┌───────────────┐                     │
│  │ 出勤時間       │  │ 退勤時間       │                     │
│  │ [08] : [00]   │  │ [19] : [30]   │                     │
│  └───────────────┘  └───────────────┘                     │
│                                                            │
│  休憩時間: [60] 分                                         │
│                                                            │
│  □ 休日出勤   □ 深夜勤務                                   │
│                                                            │
│  備考: [                                               ]   │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 💰 概算表示（保存前プレビュー）                        │  │
│  │ 実労働: 10.5h | 残業: 2.5h | 概算: ¥14,200           │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│         [キャンセル]          [🟡 保存して次へ]            │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

#### 5.3.3 一括計算画面
```
┌────────────────────────────────────────────────────────────┐
│ ← 戻る              一括計算                               │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  対象日: [2024/01/28 📅]  〜  [2024/01/28 📅]             │
│                                                            │
│  対象会社: [すべて ▼]    対象車種: [すべて ▼]              │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 対象データ: 187件                                     │  │
│  │ ├ 計算可能: 182件                                    │  │
│  │ └ エラー:    5件（車種未設定など）                    │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│          [🟡 一括計算を実行]                               │
│                                                            │
│  ════════════════════════════════════════════════════════  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 計算進捗                                              │  │
│  │ ████████████████████░░░░░ 78% (142/182)              │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ エラー一覧                                            │  │
│  │ ┌────────────────────────────────────────────────┐  │  │
│  │ │ ⚠️ 佐藤花子: 賃金ルール未設定（C社・10t）      │  │  │
│  │ │ ⚠️ 高橋健二: 退勤時間が出勤より前             │  │  │
│  │ └────────────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

#### 5.3.4 計算結果一覧（テーブル形式）
```
┌────────────────────────────────────────────────────────────────────────────┐
│ ← 戻る              計算結果一覧              2024/01/28                   │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  [🔍 検索]  会社: [すべて▼]  ステータス: [すべて▼]  [📥 CSV出力]          │
│                                                                            │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ □ │ 氏名     │ 会社  │ 車種 │ 出勤  │ 退勤  │ 残業  │ 合計     │状態│  │
│  ├───┼─────────┼──────┼─────┼──────┼──────┼──────┼─────────┼───┤  │
│  │ □ │ 山田太郎 │ A社  │ 4t  │ 08:00│ 19:30│ 2.5h │ ¥14,200 │ ✅ │  │
│  │ □ │ 鈴木一郎 │ A社  │ 2t  │ 07:00│ 18:00│ 2.0h │ ¥12,800 │ ✅ │  │
│  │ □ │ 佐藤花子 │ B社  │ 10t │ 06:00│ 20:00│ 5.0h │ ¥18,500 │ ⚠️ │  │
│  │ □ │ 高橋健二 │ A社  │ 4t  │ 08:30│ 17:30│ 0.0h │ ¥11,000 │ ✅ │  │
│  │ □ │ 田中美咲 │ C社  │ 2t  │ 09:00│ 21:00│ 3.0h │ ¥13,600 │ ✏️ │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                            │
│  選択中: 0件   [一括確定]  [一括再計算]                                    │
│                                                                            │
│  << 1 2 3 4 5 ... 10 >>                          合計: ¥2,340,000         │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

状態凡例: ✅確定済み  ⚠️要確認  ✏️手動調整有り  ⏳計算中
```

---

## 6. デザインシステム

### 6.1 カラーパレット

```css
:root {
  /* Primary - イエロー/ゴールド系（指定色） */
  --primary-50: #FFFBEB;
  --primary-100: #FEF3C7;
  --primary-200: #FDE68A;
  --primary-300: #FCD34D;
  --primary-400: #FBBF24;
  --primary-500: #F7C948;  /* メインカラー */
  --primary-600: #D97706;
  --primary-700: #B45309;

  /* Neutral - グレー系 */
  --neutral-50: #FAFAFA;
  --neutral-100: #F5F5F5;
  --neutral-200: #E5E5E5;
  --neutral-300: #D4D4D4;
  --neutral-400: #A3A3A3;
  --neutral-500: #737373;
  --neutral-600: #525252;
  --neutral-700: #404040;
  --neutral-800: #262626;
  --neutral-900: #171717;

  /* Semantic Colors */
  --success: #22C55E;
  --warning: #F59E0B;
  --error: #EF4444;
  --info: #3B82F6;

  /* Background */
  --bg-primary: #FFFFFF;
  --bg-secondary: #FAFAFA;
  --bg-tertiary: #F5F5F5;
}
```

### 6.2 タイポグラフィ

```css
/* フォント設定 */
--font-family: 'Noto Sans JP', 'Hiragino Sans', sans-serif;

/* サイズ */
--text-xs: 0.75rem;    /* 12px - 補足情報 */
--text-sm: 0.875rem;   /* 14px - テーブル内 */
--text-base: 1rem;     /* 16px - 本文 */
--text-lg: 1.125rem;   /* 18px - 小見出し */
--text-xl: 1.25rem;    /* 20px - 見出し */
--text-2xl: 1.5rem;    /* 24px - ページタイトル */
```

### 6.3 コンポーネント例

```tsx
// ボタンスタイル
<Button variant="primary">
  // bg-primary-500 hover:bg-primary-600 text-white
  保存する
</Button>

<Button variant="secondary">
  // bg-white border border-neutral-300 hover:bg-neutral-50
  キャンセル
</Button>

// カード
<Card className="bg-white rounded-lg shadow-sm border border-neutral-200 p-6">
  {children}
</Card>

// ステータスバッジ
<Badge variant="success">確定済み</Badge>  // bg-green-100 text-green-800
<Badge variant="warning">要確認</Badge>    // bg-yellow-100 text-yellow-800
<Badge variant="error">エラー</Badge>      // bg-red-100 text-red-800
```

---

## 7. ディレクトリ構成

```
eiwa-hiyatoi/
├── docs/                          # ドキュメント
│   └── 01_システム設計書.md
│
├── src/
│   ├── app/                       # Next.js App Router
│   │   ├── (auth)/               # 認証関連ページ
│   │   │   └── login/
│   │   ├── (dashboard)/          # メインアプリ
│   │   │   ├── page.tsx          # ダッシュボード
│   │   │   ├── daily-reports/    # 日報管理
│   │   │   ├── calculations/     # 計算関連
│   │   │   └── master/           # マスタ管理
│   │   │       ├── companies/
│   │   │       ├── vehicle-types/
│   │   │       ├── wage-rules/
│   │   │       └── workers/
│   │   ├── api/                  # API Routes
│   │   ├── layout.tsx
│   │   └── globals.css
│   │
│   ├── components/               # UIコンポーネント
│   │   ├── ui/                   # shadcn/ui ベース
│   │   ├── forms/                # フォームコンポーネント
│   │   ├── tables/               # テーブルコンポーネント
│   │   └── layout/               # レイアウトコンポーネント
│   │
│   ├── lib/                      # ユーティリティ
│   │   ├── db/                   # Prismaクライアント
│   │   ├── wage-calculator/      # 賃金計算エンジン
│   │   │   ├── calculator.ts
│   │   │   ├── rules.ts
│   │   │   └── types.ts
│   │   ├── validations/          # Zodスキーマ
│   │   └── utils/                # 汎用ユーティリティ
│   │
│   └── types/                    # 型定義
│
├── prisma/
│   ├── schema.prisma             # DBスキーマ
│   ├── migrations/               # マイグレーション
│   └── seed.ts                   # 初期データ
│
├── public/                       # 静的ファイル
├── tests/                        # テストコード
│   ├── unit/
│   │   └── wage-calculator.test.ts
│   └── e2e/
│
├── .env.example
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── README.md
```

---

## 8. 実装フェーズ

### Phase 1: 基盤構築（MVP）
- [ ] プロジェクトセットアップ（Next.js + Prisma + PostgreSQL）
- [ ] 認証機能（NextAuth.js）
- [ ] 基本UIコンポーネント
- [ ] マスタ管理（会社・車種・作業員）
- [ ] 日報入力画面

### Phase 2: 計算エンジン
- [ ] 賃金ルールマスタ
- [ ] 計算エンジン実装
- [ ] 一括計算機能
- [ ] 計算結果一覧・詳細

### Phase 3: 運用機能
- [ ] 計算履歴管理
- [ ] 再計算機能
- [ ] CSV出力
- [ ] ダッシュボード

### Phase 4: 改善
- [ ] パフォーマンス最適化
- [ ] エラーハンドリング強化
- [ ] ユーザビリティ改善

---

## 9. 非機能要件

### 9.1 パフォーマンス
- 200件の一括計算: 30秒以内
- 画面表示: 2秒以内
- 検索レスポンス: 1秒以内

### 9.2 セキュリティ
- HTTPS必須
- 認証必須（セッションベース）
- 入力値バリデーション
- SQLインジェクション対策（Prisma使用）

### 9.3 可用性
- 稼働時間: 平日 7:00-22:00
- バックアップ: 日次自動

---

## 10. 用語集

| 用語 | 説明 |
|------|------|
| 日報 | 作業員の1日の勤務記録 |
| 拘束時間 | 出勤から退勤までの総時間（休憩含む） |
| 残業単位 | 残業時間を丸める際の最小単位（5分、10分、15分等） |
| 賃金ルール | 会社×車種の組み合わせに対する給与計算ルール |
